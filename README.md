# Classification_of_boat_types_in_cnn
The python notebook demonstrates the use of keras to implement a convolution neural network(CNN) that classifies an image of a boat into its category(buoy,raft,...etc) 
